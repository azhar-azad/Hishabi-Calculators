info:
  name: Get Rules By {assessmentYear}
  type: http
  seq: 2

http:
  method: GET
  url: {{base_url}}/api/calculators/tax/rules/{{assessment_year}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
