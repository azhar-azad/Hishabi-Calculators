info:
  name: Get Logged In User Email
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8080/api/account/me
  auth:
    type: bearer
    token: {{bearer_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
