# Secrets
.env*

# Bruno environment files may contain auth tokens — keep local only
# Commit only *.example.yml templates so other devs know what vars to set
environments/*.yml
!environments/*.example.yml

# Dependencies
node_modules

# OS files
.DS_Store
Thumbs.db