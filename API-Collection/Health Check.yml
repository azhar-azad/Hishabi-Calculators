info:
  name: Health Check
  type: http
  seq: 1

http:
  method: GET
  url: {{base_url}}/api/health
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
